# Exchange SOTA Reproduce

This folder is generated by `scripts/extended_sota/auto_tune_extended_sota.py`.
Each wrapper contains a single CANPatchTST command that produced test MSE below the TimeMixer++ Table 16 target.

Run from this folder, for example:

```bash
bash run_Exchange_720.sh
```

Winning rows are tracked in `winning_configs.csv`.
